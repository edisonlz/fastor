from django.conf.urls.defaults import *

urlpatterns = patterns('reporting.views',
    url(r'^main$', 'report_list', name='reporting-list'),
    url(r'^(?P<slug>.*)/$', 'view_report', name='reporting-view'),
)