#coding=utf-8
__author__ = 'forxlose'


def get_start_end(page,pagesize,total):
    start = (page - 1) * pagesize
    end = page * pagesize
    if start > total:
        start = end = total
    elif end > total:
        end = total
    return start,end
